package com.camposocampoolavevargas.proyecto.data.local.model

/**
 * Enum representing the synchronization status of a database record.
 */
enum class SyncStatus {
    PENDING,
    SYNCED,
    FAILED
}

